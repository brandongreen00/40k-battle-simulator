# dependencies
node_modules/

# build output
dist/
dist-ssr/

# local env / editor
*.local
.DS_Store
.idea/
.vscode/
*.log

# Wahapedia raw CSV downloads — large + personal-use only, regenerated by `pnpm ingest`.
# The converted JSON under data/game/ IS committed so the app needs no network at runtime.
data/raw/

# headless simulator output (pnpm sim)
out/

# v2 simulator: Python bytecode and pytest cache
__pycache__/
.pytest_cache/
